    <div className="notif-item unread">
      {userData.map((items) => {
        if (items.notifType === "REACTION") {
          return (
            <div key={items.id} className="notif-item-profile-img-container">
              <img
                src={require("./" + items.profileImage + ".webp")}
                alt="profile-picture"
              />
            </div>
          );
        } else {
          return null;
        }
      })}
    </div>